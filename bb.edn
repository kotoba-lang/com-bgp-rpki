{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([bgp-rpki.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'bgp-rpki.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
